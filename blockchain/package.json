{
  "name": "proyecto-infinito-blockchain",
  "version": "1.0.0",
  "description": "InfinitoToken Smart Contract — Polygon Amoy",
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:amoy": "hardhat run scripts/deploy.js --network amoy",
    "deploy:local": "hardhat run scripts/deploy.js --network hardhat"
  },
  "keywords": [],
  "author": "Romny + Buffy (Codebuff AI)",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.0.0"
  }
}
